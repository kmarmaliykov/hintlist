TODO list
=====================================
Rest:
* роуты типа places/{page} можно выкинуть в пользу places?page={page}
* роуты /wishlist и /attendedlist заменить на /{username}/wishlist и /{username}/attendedlist
* выставление оценок местам/событиям
  /events/{type}/{name} так делать можно, если мы шлем какие-то там звездочки
  но он ооочень хочет, чтобы были еще комменты к этому делу и можно было давать ссылочку на свою оценку с комментарием
* /wishlist/{events OR places}/ вот эти роуты фиксануть, писать что-то вроде {object_type} а в комменте сказать, что принимает два значения
* "isTriedOut" : true,"isDesired"  : false, вот это сказал ему очень не нравится, лучше чтобы здесь были объекты, которые содержат дату, саму оценку и какую-то метаинфу